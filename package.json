{
  "name": "my-meesho-store",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "lucide-react": "^0.263.1",
    "firebase": "^10.0.0"
  },
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp app.jsx dist/"
  }
}
